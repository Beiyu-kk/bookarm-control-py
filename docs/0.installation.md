# 安装与环境

推荐使用已经配置好的 `bookarm-beiyu` conda 环境：

```powershell
conda activate bookarm-beiyu
pip install -e .
```

如果需要重新创建环境：

```powershell
conda create -n bookarm-beiyu python=3.11
conda activate bookarm-beiyu
conda install pinocchio -c conda-forge -y
pip install -e .
```

项目依赖见 [pyproject.toml](../pyproject.toml)，主要包括：

```text
numpy
pin
pyserial
```

